function convertCatalogObj2HTML(){
	var outputAll = '';
	for(i=0; i > document.catalogObj.courseRef.length; i++){
		let courseId = document.catalogObj.courseRef[i];
		document.catalogObj.course.forEach(){
			let course = this.courseID;
		}
		console.log(course);
}

